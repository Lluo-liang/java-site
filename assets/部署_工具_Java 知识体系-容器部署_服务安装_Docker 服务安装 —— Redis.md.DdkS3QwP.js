import{_ as s,o as d,c as a,am as i}from"./chunks/framework._FJXuPhs.js";const R=JSON.parse('{"title":"Docker 服务安装 —— Redis","description":"","frontmatter":{"title":"Docker 服务安装 —— Redis","excerpt":"摘要","date":"2025-03-30 17:00:27","updated":"2025-03-30 17:00:27"},"headers":[],"relativePath":"部署&工具/Java 知识体系-容器部署/服务安装/Docker 服务安装 —— Redis.md","filePath":"部署&工具/Java 知识体系-容器部署/服务安装/Docker 服务安装 —— Redis.md","lastUpdated":null}'),o={name:"部署&工具/Java 知识体系-容器部署/服务安装/Docker 服务安装 —— Redis.md"};function p(n,e,r,c,t,l){return d(),a("div",null,[...e[0]||(e[0]=[i(`<h3 id="安装-redis" tabindex="-1">安装 Redis <a class="header-anchor" href="#安装-redis" aria-label="Permalink to &quot;安装 Redis&quot;">​</a></h3><p>安装 Redis 使用 Docker 是一个非常简单且快捷的过程。以下是通过 Docker 安装和运行 Redis 的基本步骤：</p><ol><li><p><strong>拉取 Redis 镜像</strong>： 首先，你需要从 Docker Hub 拉取最新的 Redis 镜像。打开命令行界面，然后输入以下命令：</p><p><code>docker pull redis</code></p><p>这个命令会从 Docker Hub 下载最新的 Redis 镜像。</p></li><li><p><strong>运行 Redis 容器</strong>： 接着，使用以下命令来启动一个 Redis 容器：</p><p><code>docker run --name redis -d redis</code></p><p>这里，<code>--name redis</code> 设置容器的名称为 <code>some-redis</code>。你可以根据需要更改这个名称。<code>-d</code> 参数表示容器将在后台运行。</p></li><li><p><strong>暴露和映射端口</strong>（可选）： 如果你打算从宿主机外部访问 Redis，你需要映射端口。Redis 默认端口是 <code>6379</code>，所以你可以这样映射端口：</p><p><code>docker run --name redis -d -p 6379:6379 redis</code></p><p><code>-p 6379:6379</code> 表示将容器的 6379 端口映射到宿主机的 6379 端口。</p></li><li><p><strong>访问 Redis</strong>： 一旦 Redis 容器在运行，你可以通过 Redis 客户端或任何支持 Redis 的程序访问它。如果你只是在本地机器上运行和访问 Redis，你可以通过 <code>localhost:6379</code> 来访问它。</p></li><li><p><strong>使用 Redis CLI 进行交互</strong>（可选）： 如果你想要通过 Redis 命令行界面与 Redis 容器交互，可以使用以下命令进入 Redis 容器：</p><p><code>docker exec -it some-redis redis-cli</code></p></li></ol><p>小结：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>docker pull redis</span></span>
<span class="line"><span></span></span>
<span class="line"><span>docker run --name redis -d -p 6379:6379 redis</span></span>
<span class="line"><span></span></span>
<span class="line"><span>firewall-cmd --permanent --add-port=6379/tcp</span></span>
<span class="line"><span>firewall-cmd --reload      # 重启防火墙</span></span></code></pre></div>`,5)])])}const k=s(o,[["render",p]]);export{R as __pageData,k as default};
